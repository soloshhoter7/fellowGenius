import { DateFormatDdmmyyyyDirective } from './date-format-ddmmyyyy.directive';

describe('DateFormatDdmmyyyyDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFormatDdmmyyyyDirective();
    expect(directive).toBeTruthy();
  });
});
